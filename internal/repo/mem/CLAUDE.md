# internal/repo/mem/

| File | When to Read |
|------|-------------|
| `doc_repo.go` | In-memory `DocRepo` — map-backed document storage for tests |
| `config_repo.go` | In-memory `ConfigRepo` — holds a `*domain.Config` for tests |
| `brief_repo.go` | In-memory `BriefRepo` — returns a preset `*domain.Brief` for tests |
| `iteration_repo.go` | In-memory `IterationRepo` — slice-backed iteration storage for tests |
| `state_repo.go` | In-memory `StateRepo` — holds a `*domain.WorkflowState` for tests |
