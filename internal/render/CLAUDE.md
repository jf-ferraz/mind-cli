# internal/render/

| File | When to Read |
|------|-------------|
| `render.go` | `Renderer` with three `OutputMode`s (interactive, plain, JSON); `DetectMode()` for TTY detection; `Render*()` methods for all domain output types |
