# Mind Agent Framework

> **All framework content lives in `.mind/`.** This file is the Claude Code auto-loaded routing table.

Read `.mind/CLAUDE.md` for the full resource index with all agent paths, workflow types, and quality gates.

| Command | Description |
|---------|-------------|
| `/analyze "topic"` | Structured multi-persona analysis → convergence document |
| `/discover "idea"` | Interactive exploration → project brief |
| `/workflow "description"` | Full agent pipeline → implementation |
| `/init` | Guided setup — detect missing docs, create what's needed |
